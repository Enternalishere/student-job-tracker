# Student Job Tracker
A MERN stack app to track job applications.

## Setup
- Backend: `cd backend && npm install && npm start`
- Frontend: `cd frontend && npm install && npm start`
- MongoDB: Set up Atlas and add MONGO_URI to backend/.env

## Deployment
- Frontend: Vercel
- Backend: Render
- Database: MongoDB Atlas
